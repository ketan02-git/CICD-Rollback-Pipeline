apiVersion: v1
kind: Service
metadata:
  name: demo-app
spec:
  type: {{ .Values.service.type }}
  selector:
    app: demo-app
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 3000
      nodePort: {{ .Values.service.nodePort }}